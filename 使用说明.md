# dutflow 中文使用说明

本文说明 Windows、Arch Linux 与公网服务器三方的部署和日常操作。示例中的主机名、用户名、地址和域名均为占位符，请以自己的私有配置为准。

> 日常使用只需在 Arch Linux 上执行 `dutflow ssh` 或 `dutflow stream`。Moonlight 配对成功后，不必每次输入 PIN。

## 1. 功能与连接方式

- **Windows 本机**：被控制端，运行 OpenSSH、Sunshine，并定期上报内网 IP。
- **Arch Linux**：控制端，通过 `dutflow` 查询地址、登录 SSH、提交配对 PIN 和启动 Moonlight。
- **公网服务器**：仅提供地址上报与查询，不中转 SSH、桌面音视频或输入控制流量。

```text
Windows ── HTTPS 上报内网 IP ──> 公网服务器
Arch    ── HTTPS 查询内网 IP ──> 公网服务器

Arch ── 内网 SSH / Moonlight ──> Windows
```

**使用前提：** Windows 已开机，双方有可互通的内网路径，并且可以访问地址查询服务。不同 IP 网段不一定不能互通，应以实际连接测试为准。

本项目不建立 VPN、反向隧道或公网串流中继，也不负责绕过局域网隔离。地址发现成功不代表 SSH 或串流一定可达。

## 2. 配置位置与验证清单

| 项目 | 位置或名称 |
| --- | --- |
| Windows 部署目录 | `C:\ProgramData\Dutflow` |
| Windows SSH 服务 | `sshd` |
| Windows Sunshine 服务 | `SunshineService` |
| Windows 地址上报任务 | `DutflowAnnounce` |
| Arch 命令 | `/usr/local/bin/dutflow` |
| Arch 配置 | `~/.config/dutflow/.env` |
| Arch SSH 专用私钥 | `~/.ssh/dutflow_windows` |
| 公网地址服务 | 自行配置的 HTTPS URL |
| 公网服务名 | `dutflow-rendezvous.service` |

安装后请逐项验证：

- `dutflow ssh hostname` 返回预期的 Windows 主机名。
- `dutflow check` 返回 `Sunshine authentication OK`。
- Moonlight 能列出应用，并在图形会话中建立串流。
- 实测画面、键鼠交互及长时间稳定性。
- 执行自启脚本后，重启并确认无人登录时仍能上报地址。

这些是验证步骤，不代表新的安装已经通过测试。原部署的私人测试记录保留在未跟踪的 `.local/` 中，不随开源快照发布。

## 3. 日常使用：在 Arch 上执行

### 3.1 查询 Windows 内网 IP

```bash
dutflow ip
```

不带参数执行 `dutflow` 也会查询地址。Windows 正常运行上报循环时约每 30 秒更新一次；服务器超过 180 秒没有收到上报，就不再返回该记录。

换网或 IP 变化后先等一次上报，再重新执行命令。正在进行的 SSH 或串流不会自动迁移到新地址，需要重新连接。

### 3.2 SSH 登录与执行命令

```bash
# 进入 Windows SSH 会话
dutflow ssh

# 执行命令并返回
dutflow ssh hostname

dutflow ssh whoami
```

退出交互式 SSH 会话时输入：

```text
exit
```

客户端默认使用 `~/.ssh/dutflow_windows`，私钥只应保存在 Arch 上，不要复制到公网服务器或提交到 Git。

### 3.3 检查 Sunshine 管理接口认证

```bash
dutflow check
```

正常输出：

```text
Sunshine authentication OK
```

该检查通过 SSH 在 Windows 上调用本机 Sunshine API。它验证认证可用，不等于已经建立串流。

### 3.4 启动桌面串流

在 **Arch 已登录的图形桌面终端**执行：

```bash
# 默认串流 Desktop
dutflow stream

# 显式指定应用
dutflow stream Desktop

# 指定画面参数
dutflow stream Desktop --1080 --fps 60

# 低负载参数示例
dutflow stream Desktop --720 --fps 30 --bitrate 5000 --display-mode windowed --no-quit-after
```

带参数时，请显式写出 `Desktop` 或其他应用名；当前脚本只有在 `stream` 后完全没有参数时才自动补上 `Desktop`。

列出应用、启动其他应用：

```bash
moonlight list "$(dutflow ip)"
dutflow stream "Steam Big Picture"
```

应用名必须与 Sunshine 提供的列表一致。更多客户端参数可通过下面的命令查看：

```bash
moonlight stream --help
```

普通 SSH 会话不一定具备 Arch 图形会话环境；出现显示服务错误时，请先在 Arch 桌面终端运行，而不是直接在无图形环境中启动串流。

## 4. 首次配对或更换 Moonlight 客户端

**如果客户端已经配对，日常使用跳过本节。** 不要为了测试而删除正常的现有配对。

需要配对新客户端时，在 Arch 桌面打开两个终端。下面的 `1234` 只是示例，请自行选择本次使用的四位 PIN。

终端一：

```bash
moonlight pair --pin 1234 "$(dutflow ip)"
```

保持配对请求处于等待状态，在终端二执行相同的 PIN：

```bash
dutflow pin 1234
```

`dutflow pin` 会通过 SSH 在 Windows 上执行 `sunshine-pin.ps1`，使用已保存的 Sunshine 凭据调用本机 API。成功提交后还应确认 Moonlight 显示配对成功，再执行：

```bash
moonlight list "$(dutflow ip)"
dutflow stream Desktop
```

如果提示 `Sunshine rejected PIN; check that Moonlight pairing is pending`：检查 Moonlight 是否正在等待配对、PIN 是否一致，以及客户端是否早已配对。**没有待处理请求时，单独提交 PIN 不会成功。**

## 5. Windows 开机后台自启动

### 5.1 设置方法

使用当前 Windows 账户打开 **管理员 PowerShell**，执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\windows\enable-startup.ps1
```

以上命令从仓库根目录运行；如果脚本已复制到部署目录，也可以使用其绝对路径运行。此脚本依赖已有部署，不是从零安装脚本。

它会：

1. 检查部署文件和配置是否存在，并备份已有计划任务定义。
2. 将 `sshd`、`SunshineService` 设为自动启动并启动服务。
3. 将 `DutflowAnnounce` 设为 SYSTEM 身份、开机触发、后台隐藏运行。
4. 取消任务运行时限及电池供电限制，并配置异常退出后的重启策略。
5. 立即启动任务，检查任务是否仍在运行。

脚本不会改动 Token、Sunshine 凭据、SSH 公钥或防火墙，也不会重启电脑。成功时显示：

```text
SUCCESS: SYSTEM startup task is running in the background.
```

### 5.2 检查状态

```powershell
Get-Service sshd, SunshineService | Format-Table Name, Status, StartType

$task = Get-ScheduledTask -TaskName DutflowAnnounce
$task | Select-Object TaskName, State
$task.Principal | Select-Object UserId, LogonType, RunLevel
$task.Triggers | Select-Object CimClass
$task.Settings | Select-Object ExecutionTimeLimit, DisallowStartIfOnBatteries, StopIfGoingOnBatteries
```

预期结果：服务为 `Running / Automatic`；任务为 `Running`，用户为 `SYSTEM` 或相应 SID，触发器为 `MSFT_TaskBootTrigger`；运行时限为 `PT0S`，两个电池限制字段均为 `False`。

脚本成功只表示设置已应用、任务已启动，不代表公网服务器已经收到最新上报。请再从 Arch 执行 `dutflow ip` 检查。

### 5.3 重启验证

方便时重启 Windows，先不登录，在 Arch 上执行：

```bash
dutflow ip
dutflow ssh hostname
```

这可以验证开机后无人登录时的地址发现及 SSH。串流需要另行测试，不能把服务自动启动等同于已验证锁屏或未登录状态下的画面捕获。

## 6. 配置与凭据维护

### Arch 配置

`~/.config/dutflow/.env` 包含以下变量：

```bash
DUTFLOW_URL=https://rendezvous.example.com
DUTFLOW_TOKEN=已有的地址服务令牌
DUTFLOW_SSH_USER=WINDOWS_USERNAME
# 可选；未指定时使用下面的默认路径
DUTFLOW_SSH_KEY="$HOME/.ssh/dutflow_windows"
```

以上是结构示例，**不要用示例值覆盖现有 Token**。配置文件会作为 Shell 脚本载入，只能使用可信内容；权限应保持为 `600`。

可通过 `DUTFLOW_CONFIG` 指定其他配置文件路径。未指定时优先读取 `.env`，不存在则兼容旧的 `~/.config/dutflow/config`。如果显式指定的文件不存在，将报错，不会回退到其他配置。

### Windows 配置与 Sunshine 凭据

- 地址服务配置：`C:\ProgramData\Dutflow\config.json`。
- Sunshine 凭据：`C:\Users\WINDOWS_USERNAME\.ssh\dutflow-sunshine.xml`，使用 Windows 用户及本机绑定的加密方式保存。
- Sunshine PIN 脚本通过 SSH 以 `WINDOWS_USERNAME` 身份运行；不要把它改成 SYSTEM 运行来读取此用户凭据。

Sunshine 管理密码变更后，使用 **WINDOWS_USERNAME 账户的管理员 PowerShell**重新执行：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\ProgramData\Dutflow\complete-access.ps1
```

输入的是 **Sunshine 网页管理界面的用户名和密码，不是 Windows 密码**。脚本保留已有 SSH 公钥，更新加密凭据并检查认证。无需把密码发给其他人。

不要将配置中的 Token、私钥、凭据文件或它们的完整内容提交到 Git、聊天或公开日志。

## 7. 常见故障排查

| 现象 | 检查与处理 |
| --- | --- |
| `Local PC is offline or not announced` / 查询 HTTP 404 | 检查 Windows 是否开机、`DutflowAnnounce` 是否运行、上报是否超过 180 秒未更新；启动后稍等再查询。 |
| 地址服务返回 HTTP 401 | 检查两端地址服务 Token 是否一致；不要与 Sunshine 密码混淆。 |
| `No route to host` / 连接超时 | 检查实际网络路由、接入网络隔离及 Windows 防火墙；同一校园或同一网络名称也不能保证互通。 |
| `Permission denied (publickey,...)` | 检查 Arch 专用私钥、SSH 用户和 Windows 授权公钥；管理员账户使用的授权文件与普通账户不同。 |
| `Host key verification failed` | 核对主机密钥，确认目标确实是 Windows；不要直接关闭密钥验证。IP 变化也可能需要重新核对信任记录。 |
| `dutflow check` 认证失败 | 确认 Sunshine 正在运行、凭据仍有效，并以原 Windows 用户执行；必要时重跑 `complete-access.ps1`。 |
| PIN 被拒绝 | 先发起 Moonlight 配对，再提交相同 PIN；已配对时直接串流。 |
| Moonlight 找不到显示服务 | 在 Arch 图形桌面终端启动；普通 SSH 环境不能替代桌面会话环境。 |
| 查询到了 VMware 等虚拟网卡地址 | 当前上报脚本并未可靠排除全部虚拟网卡，多网卡时应核查选址；不要把地址服务成功当成链路验证成功。 |

Windows 手动触发一次上报：

```powershell
powershell.exe -NoProfile -ExecutionPolicy Bypass -File C:\ProgramData\Dutflow\announce-once.ps1
```

管理员 PowerShell 中重启地址上报任务：

```powershell
Stop-ScheduledTask -TaskName DutflowAnnounce
Start-ScheduledTask -TaskName DutflowAnnounce
Get-ScheduledTaskInfo -TaskName DutflowAnnounce
```

公网服务器检查命令，在已配置 `PUBLIC_SERVER_ALIAS` SSH 别名的终端执行：

```bash
ssh PUBLIC_SERVER_ALIAS 'systemctl status dutflow-rendezvous.service --no-pager'
ssh PUBLIC_SERVER_ALIAS 'journalctl -u dutflow-rendezvous.service -n 50 --no-pager'
curl --fail https://rendezvous.example.com/healthz
```

健康接口成功返回 `{"ok": true}` 仅表示服务可达，不证明 Windows 已上报或流量可直连。

## 8. 当前边界与限制

- 公网服务器不是中继，但当前 `dutflow` 每次执行都会查询它，因此地址服务不可达时命令会失败；没有离线地址缓存。
- 当前服务器只保存一个 Windows 地址，没有实现多网卡候选地址与可达性探测。
- 当前客户端没有对查询结果做严格的内网地址或路由校验。部署按内网地址直连设计，但这不等于已经实现强制阻止公网路径的安全检查。
- Windows 关机、休眠或网络断开时，本项目不保证能唤醒或访问它。
- 自启任务不保证任何锁屏、未登录、多显示器或显卡状态下都能正常捕获桌面，应按实际环境验证。
- 本文按现有部署编写；迁移到新电脑时还需要安装软件、重新建立密钥信任及配置，不应照搬旧凭据文件。

## 9. 一页速查

```bash
# 以下均在 Arch 执行
dutflow ip                       # 查询 Windows 地址
dutflow ssh                      # 登录 Windows
dutflow ssh hostname             # 验证 SSH
dutflow check                    # 检查 Sunshine 认证
moonlight list "$(dutflow ip)"     # 列出串流应用
dutflow stream                   # 串流 Desktop
dutflow stream Desktop --1080 --fps 60
```

平时不需要操作公网服务器，不需要修改固定 IP，也不需要重复配对。

## 10. 初次安装与开源配置

先安装 Windows OpenSSH Server 和 Sunshine、Arch 的 Bash/curl/OpenSSH/Moonlight，以及公网服务器的 Python 3、OpenSSL、systemd 和 HTTPS 反向代理。以下步骤不会替你安装这些依赖，也不会自动配置公网 DNS 或证书。

### 公网服务器

在服务器的仓库根目录执行：

```bash
(cd server && sudo bash install.sh)
```

安装器将配置写入 `/etc/dutflow/dutflow.env`，令牌保存在 `/etc/dutflow/token`，两者权限均为 `600`，目录权限为 `700`。新安装的后端绑定 `127.0.0.1:18787`；按 `server/nginx-dutflow.conf` 模板配置自己的域名和有效 HTTPS 证书。脚本不输出令牌，需要通过安全方式把同一个令牌配置到 Windows 和 Arch。

已运行的服务不会因 `enable --now` 自动重新加载配置；更新现有服务器配置时，应在安排好的维护窗口自行重启并验证服务。本次开源整理未重新部署现有服务器。

### Windows 私有配置

在仓库根目录执行（仅首次创建，勿覆盖已有私有配置）：

```powershell
Copy-Item .\windows\config.example.json .\windows\config.local.json
notepad .\windows\config.local.json
# 填入自己的 HTTPS URL、token 和 Windows SSH 用户后，以管理员执行：
powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\windows\install.ps1
```

默认安装器从 `windows/config.local.json` 读取值，也可通过 `-ConfigFile` 指定其他私有 JSON 路径。尽量不要用 `-Token` 参数传递令牌，以免留在命令行或历史记录中。运行配置仍位于 `%ProgramData%\Dutflow\config.json`，Sunshine 密码继续使用加密凭据文件，不需要转成明文 `.env`。

初始化 SSH/Sunshine 凭据前，先将自己的 Arch 公钥（不是私钥）安全复制到 `%ProgramData%\Dutflow\arch-authorized.pub`，然后以目标 Windows 管理员账户执行仓库的 `windows/complete-access.ps1`。该辅助脚本针对管理员账户写入 `administrators_authorized_keys`；普通账户需按 OpenSSH 的账户规则单独配置。最后按第 5 节设置开机后台任务。

### Arch 私有配置

在仓库根目录执行（仅首次创建）：

```bash
sudo install -Dm755 archlinux/dutflow /usr/local/bin/dutflow
install -d -m700 "$HOME/.config/dutflow"
install -m600 .env.example "$HOME/.config/dutflow/.env"
# 编辑 ~/.config/dutflow/.env，填写自己的配置
```

根目录 `.env` 是此次整理保存的本地私有配置备份，不会被客户端自动读取；需要明确设置 `DUTFLOW_CONFIG` 或安装到上述路径。不要把示例占位符当成可用的凭据。

### 发布前

`.env`、`windows/config.local.json` 和 `.local/` 已忽略；`.gitignore` 不是加密或文件访问控制，请限制本机文件访问权限，也不要把整个工作目录打包公开。发布流程和历史风险见 `开源发布说明.md`，本项目采用 `LICENSE` 中的 MIT 协议。
