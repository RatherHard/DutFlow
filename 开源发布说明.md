# 开源发布说明

本仓库已将部署所需的敏感值与公开代码分离，适合在检查后制作公开快照。

## 已完成的隔离

- 根目录 `.env`、`windows/config.local.json` 和 `.local/` 用于本地私有资料，已加入 `.gitignore`。
- `.env.example` 与 `windows/config.example.json` 只包含占位符。
- Arch 客户端支持 `~/.config/dutflow/.env`，并兼容旧的 `~/.config/dutflow/config`。
- Windows 安装器默认读取未跟踪的 `windows/config.local.json`。
- 公网服务器安装器在 `/etc/dutflow` 私下生成 token 和环境文件，不再把 token 打印到安装输出。
- `LICENSE` 使用 MIT 协议。

## 发布前检查

```bash
git status --short
# 检查所有被跟踪的路径，示例配置应保留，私有配置不应出现
git ls-files
git check-ignore -v .env windows/config.local.json .local/README.md
bash tests/client.sh
python -m unittest discover -s tests -p 'test_*.py'
```

还应人工检查公开文档、示例、脚本和构建产物，不要把 token、密码、私钥、主机名、用户名、真实域名或内网地址写进提交。若任何凭据曾经出现在公开日志、聊天记录或共享仓库中，请在发布前轮换；不要因为本说明自动修改正在运行的部署。

## 既有 Git 历史

`.gitignore` 不会清除以前的提交。本次检查确认：清理前的 6 个历史提交中均含已知真实部署标识（域名、用户名或地址等）。不要直接把当前仓库历史推到公开远端。针对当前在用 token 的精确搜索未在这 6 个提交中发现匹配，但这不等于完整的历史凭据审计，也不能排除旧凭据或未知敏感信息。Git 提交的作者姓名、邮箱等元数据亦应单独检查。推荐从清理后的提交制作一个不含 `.git` 的新快照：

```bash
# 在原仓库根目录执行；输出文件位于上一级目录
git archive --format=zip --output=../dutflow-public.zip HEAD
```

将压缩包解压到新的目录，重新检查敏感信息，然后在新目录执行：

```bash
git init
git add .
git commit -m "Initial public release"
```

不要覆盖原仓库，也不要把原仓库的 `.git` 目录复制到公开目录。若必须保留历史，应先使用经过审阅的历史重写工具清除所有敏感对象，并在清除后强制轮换曾经暴露的凭据。

## 第三方软件

OpenSSH、Sunshine、Moonlight、curl、Python 和系统服务仍受其各自许可证与条款约束；本仓库的 MIT 许可不替代第三方许可证。