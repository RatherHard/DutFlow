## 基础需求

1. 两台电脑位于同一内网，但各自的内网 IP 会不定期变化。
2. 需要在两台电脑之间建立单向的内网直连信道，使一台电脑能主动连接另一台。
3. 该信道用于随时进行 Sunshine/Moonlight 串流，以及通过 SSH 连接 agent。
4. 需要借助一台公网服务器辅助实现，但公网服务器仅用于解决内网 IP 变化导致的发现或定位问题。
5. 实际串流和 SSH 流量必须只经过内网，从一台电脑直达另一台电脑，不得经过公网服务器中转。
6. 不接受建立新的公网中继信道，也不接受流量绕行公网。
7. 搭建过程应快速，初始配置完成后，只需要一台电脑发起请求另一台电脑就能自动响应。
8. 当任一电脑的内网 IP 变化后，无需人工重新配置另一台电脑，仍能重新建立连接。
9. 本机提供 sunshine ，另一台使用 archlinux 的电脑负责连接。
10. 你需要准备本机、公网服务器和另一台 archlinux 的电脑的脚本，并把脚本部署到公网服务器和本机上，archlinux 那边你只需要先提供部署方案，计划由我自行部署。

## 本机需求

1. 本机是被连接方，另一台使用 archlinux 的电脑将连接本机（串流、ssh）。
2. 首先本机能够作为 ssh 服务器被另一台使用 archlinux 的电脑访问并操控。
3. 然后在 ssh 连接的基础上，另一台使用 archlinux 的电脑能直接远程完成本机 sunshine 串流 pin 码的配置（使用 curl 完成）。
4. 只要本机处于开机状态且处于和另一台使用 archlinux 的电脑相同的内网下，本机就能被那边任意访问并操控。

## 使用 archlinux 的电脑的需求

1. 配备一套 dutflow 命令，支持查询本机内网 ip、直接连接本机 ssh、直接启动本机 sunshine 并成功完成串流。

## 公网服务器需求

1. 能够接收本机发送的本机内网地址。
2. 能够在使用 archlinux 的电脑请求时向其发送本机内网地址。

## 可用资源

1. ssh PUBLIC_SERVER_ALIAS 可以连接公网服务器。
1. ssh ARCH_CLIENT_ALIAS 可以连接使用 archlinux 的电脑。